
ALTER TABLE activities ADD COLUMN IF NOT EXISTS "status" text DEFAULT 'Proposed';
-- Update existing rows to have 'Ongoing' as default status per requirement
UPDATE activities SET status = 'Ongoing' WHERE status IS NULL;
